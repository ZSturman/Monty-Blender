[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (may require server)
    requires_blender: Tests that require Blender server running

# Default to running only unit tests
addopts = -m "unit" -v